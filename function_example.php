<?php
    declare(strict_types=1);

    function getMax(int $x, int $y): int {
    	return $x + $y;
    }

    $a = getMax($a, 5) + getMax((1 + 2), (3 + 4));

    $a = 5;

    test0();

    getMax(1, 2);

    3;
?>